# Hello word assignment

Create a program that prints "Hello World!"

Also create a `Makefile` to build the program.

## Starting point

Start from the following skeleton:

```c=
#include<stdio.h>

int main(void){
    ...
}
```

## Build

Build th program using:

```
make
```

## Test

To test, run:

```console=
./helloworld | grep 'Hello, world!'
```

**bold**

*italic*

`typewriter`

~~strikethrough~~

## Bonus: Python

As bonus, create  a similare program in Python. Start from the folllowing skeleton:

```py=
import sys

def main():
    ...    # Add your code here
    
if __name__b == "__main__":
    sys.exit(main())
```